# 07 — LLM applications

A useful application adds prompts, output contracts, validation, and error handling around a model. Structured output is a protocol, not magic intelligence.

Run the example without a model. **Experiment:** change the instruction and validate a missing field.
